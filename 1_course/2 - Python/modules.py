#!/bin/python3

import sys # system functions & parameters (here we import all modules). #
from datetime import datetime as dt# TIME functions import with alias (here we import one module). #
import os # provides functions for interacting with the operating system. #

print(sys.version)
print(dt.now())
os.system('ls')
